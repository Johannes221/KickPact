{
  "version": "0.0.1-stub",
  "generatedAt": "2026-05-22T00:00:00.000Z",
  "_note": "Stub manifest — will be regenerated by `npm run fixtures:refresh` (Phase 1 capture pipeline). Permissive defaults so drift:check passes locally until real manifest is captured.",
  "scraperFunctions": {
    "searchVereine": {
      "expectedFields": {
        "items[].name": { "type": "string", "minLength": 1 },
        "items[].slug": { "type": "string", "minLength": 1 },
        "items[].vereinId": { "type": "string", "minLength": 1 }
      },
      "domAnchors": []
    },
    "getMannschaften": {
      "expectedFields": {
        "items[].name": { "type": "string", "minLength": 1 },
        "items[].teamId": { "type": "string", "minLength": 1 },
        "items[].slug": { "type": "string", "minLength": 1 }
      },
      "domAnchors": []
    },
    "getSpiele": {
      "expectedFields": {
        "items[].spielId": { "type": "string", "minLength": 1 }
      },
      "domAnchors": []
    },
    "getKader": {
      "expectedFields": {},
      "domAnchors": []
    },
    "getSpielDetails": {
      "expectedFields": {},
      "domAnchors": []
    }
  }
}
