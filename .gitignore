# deps
node_modules/

# next
.next/
out/

# env
.env
.env.local
.env*.local

# logs
*.log
npm-debug.log*

# test
coverage/

# editor
.vscode/
.idea/
.DS_Store

# drizzle (lokale snapshots)
drizzle/meta/_journal.json.bak

# legacy (alter April-Code, nur lokal als Referenz)
reference/

# inngest dev
.inngest/

# claude code agent worktrees (lokal, isoliert)
.claude/worktrees/

# typescript incremental build cache
*.tsbuildinfo

# playwright test artifacts
test-results/
playwright-report/
.playwright-cache/
.e2e-artifacts/

# drift checker reports (regenerated on each run)
drift-report.json
drift-report.md

# Sentry — generiert beim Build, enthält SENTRY_AUTH_TOKEN
.env.sentry-build-plugin

# Playwright MCP — console/snapshot dumps aus dev-debug
.playwright-mcp/
